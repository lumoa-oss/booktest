# ignore python environments
venv

# ignore temporary files
.tmp

# booktest configuration file
.booktest

# coverage
.coverage
.coverage.*
coverage.xml

# idea project folder
.idea

# temporary python files
__pycache__

# temporary testing related files
.out

# generated by git
*.orig

# locks used by tools
.~lock*

# pipy
build
dist
*egg-info/

.env
.env.*

!.env.example

# DVC-related files
# Local cache of snapshot content (can be large, should not be in Git)
.booktest_cache/
# Temporary DVC config file (user-specific)
.booktest.dvc

# Note: booktest.manifest.yaml should be tracked in Git (not ignored)
# Note: .dvc/ directory should be tracked in Git (DVC has its own .dvc/.gitignore)
