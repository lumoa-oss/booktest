# Booktest Environment Configuration Example
# Copy this file to .env and fill in your values

# =============================================================================
# OpenAI / Azure OpenAI Configuration (for AI-assisted review)
# =============================================================================
# Required for using reviewln(), treviewln(), ireviewln() and other AI review features

# API Key (required)
# Get from: https://platform.openai.com/api-keys (OpenAI)
# Or from Azure Portal (Azure OpenAI)
export OPENAI_API_KEY=your-api-key-here

# Azure OpenAI Endpoint (required for Azure)
# Format: https://your-resource-name.openai.azure.com
# Leave empty if using standard OpenAI
export OPENAI_API_BASE=https://your-resource.openai.azure.com

# Model Name (required)
# Examples: gpt-4, gpt-4-turbo, gpt-35-turbo, gpt-4o
export OPENAI_MODEL=gpt-4

# Azure Deployment Name (required for Azure)
# The name you gave your deployment in Azure Portal
# Default: gpt35turbo (if not specified)
export OPENAI_DEPLOYMENT=gpt35turbo

# API Version (required for Azure)
# Example: 2024-08-01-preview, 2024-02-15-preview
# See: https://learn.microsoft.com/en-us/azure/ai-services/openai/api-version-deprecation
export OPENAI_API_VERSION=2024-08-01-preview

# Max Completion Tokens (optional)
# Maximum tokens for LLM responses
# Default: 1024 (if not specified)
export OPENAI_COMPLETION_MAX_TOKENS=1024

# =============================================================================
# Test Configuration (optional)
# =============================================================================
# Add any custom environment variables your tests need

# Example: Test server hostname
export HOST_NAME=https://httpbin.org/anything

# Example: Test environment variable
export TEST_ENV_VARIABLE=hello
